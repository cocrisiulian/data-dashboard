# Lab 4 — ORM (Code-First)

Files:
- `lab4.pdf` — lab description (ORM code-first approach)

What to do (summary / checklist):

- Read the lab PDF to understand the code-first expectations.
- Create ORM model classes first, then generate the database schema from them.
- Provide migration scripts and sample seeding data.
- Provide examples showing how to run migrations and use the models.

Suggested deliverables:
- `models/` (ORM class definitions)
- `migrations/` (migration files)
- `examples/seed.sql` or `examples/seed.js`

Next steps:
1. Extract required entities and fields from the PDF.
2. Implement model definitions and show migration steps.
