# Lab 3 — ORM (Schema-First)

Files:
- `lab3.pdf` — lab description (ORM schema-first approach)

What to do (summary / checklist):

- Read the lab PDF to understand schema-first ORM tasks.
- Create the database schema (DDL) and then generate ORM models from it.
- Provide code examples showing how to use the ORM with the generated models.
- If applicable, provide migration scripts and sample queries.

Suggested deliverables:
- `schema/schema.sql`
- `orm/models/` (generated models)
- `examples/usage.md`

Next steps:
1. Extract exact model/fields from the PDF and map them to ORM models.
2. Generate code snippets and sample CRUD operations.
